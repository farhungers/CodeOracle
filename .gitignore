.env
.env.local
.venv/
__pycache__/
*.pyc
logs/
_probe/
.claude/settings.local.json

# Runtime append-only state is now committed back by CI so state persists
# across GitHub Actions runs (see .github/workflows/cycle.yml). Was gitignored
# when the scanner ran locally on Task Scheduler.
